services:
  - type: web
    name: memory-mines
    env: node
    buildCommand: npm install
    startCommand: node server/index.js
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 3000
